Реализовать функцию func Calc(expression string) (float64, error) expression - строка-выражение состоящее из односимвольных идентификаторов и знаков арифметических действий Входящие данные - цифры(рациональные), операции +, -, *, /, операции приоритезации ( и ) В случае ошибки записи выражения функция выдает ошибку.

Решение:

Будут функции:
    get_left_bracket([]string) (int) - для поиска крайней левой (
    get_right_bracket([]string) (int) - для поиска крайней правой )

Шаги выполнения:

Может начать поиск скобок с середины
    - Убрать лишние пробелы
    - Преобразование строки expression в слайс строк символов
    - Поиск первой крайней левой (
    - Поиск первой крайней правой )
        - - Взять такую строку в стек
        - - Проверить на вторую вложенность строк
        - - - Поиск первой крайней левой (
        - - - Поиск первой крайней правой )
        - - - Занести вторую вложенность в стек
        - - - Выполнить операции
        - - Вернуть результат операций в прошлый стек, вырезав операцию

        - Счёт первой вложенности
        - Вернуть результат операций в прошлый стек, вырезав операцию

    - вернуть результат вложенности
    // теперь не должно быть скобок

    - вычисляем всю последовательность по 